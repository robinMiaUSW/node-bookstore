<style>
  .title-box {
    display: flex;
    justify-content: space-between;
    margin-bottom: 2rem;
  }
</style>



<% if (users?.length> 0) { %>
  <!-- <table class="table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Full Name</th>
        <th scope="col">Email</th>
        <th scope="col">Role</th>
        <th scope="col">Action</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach((userItem, i)=> { %>
        <tr>
          <th scope="row">
            <%= i + 1 %>
          </th>
          <td>
            <%= userItem.name %>
          </td>
          <td>
            <%= userItem.email %>
          </td>
          <td>
            <%= userItem.role %>
          </td>
          <td>
            <a href="/admin/users/changeRole/<%= userItem._id %>" class="btn btn-secondary btn-sm">Change Role</a>
            <a href="/admin/users/delete/<%= userItem._id %>" class="btn btn-danger btn-sm">Delete User</a>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table> -->
  <div class="card same-height">
    <div class="card-body">
      <div class="title-box my-3">
        <h5>User List</h5>
        <a href="/admin/users/create" class="btn btn-primary btn-sm">
          Add User
        </a>
      </div>
      <!-- <h5>User Information</h5> -->
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(user=> { %>
            <tr>
              <td>
                <%= user.name %>
              </td>
              <td>
                <%= user.email %>
              </td>
              <td><span class="badge <%= user.role === 'admin' ? 'bg-success' : 'bg-warning' %> text-capitalize">
                  <%= user.role %>
                </span>
              </td>
              <td><a href="/admin/users/changeRole/<%= user._id %>" class="btn btn-secondary btn-sm">Change Role</a>
              </td>
            </tr>
            <% }) %>
        </tbody>
      </table>
    </div>
  </div>
  <% } else { %>
    <h6>No Users</h6>
    <% } %>